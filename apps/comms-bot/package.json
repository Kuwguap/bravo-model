{
  "name": "@speedy/comms-bot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Communications bot (Render): Facebook Messenger lead-gen — human conversation collects tag details into a live sheet, takes payment (chat link or on-site), and hands finished leads to the dispatch bot.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "@speedy/shared": "file:../../packages/shared",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "stripe": "^17.5.0"
  }
}
